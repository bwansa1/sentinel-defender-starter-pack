AuditLogs
| where TimeGenerated > ago(30d)
| where OperationName has_any ("Disable Strong Authentication", "Update user")
| extend Actor = tostring(InitiatedBy.user.userPrincipalName)
| project TimeGenerated, Actor, TargetResources, OperationName
| order by TimeGenerated desc